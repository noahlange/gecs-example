# @gex/example-intersections

A basic example for gecs demonstrating the basic use of systems, entities, components and multiple plugins.

The plugin instance can be accessed via `ctx.$.intersections`.
